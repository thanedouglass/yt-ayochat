# Server-only settings used by the /app/api route handlers that proxy to FastAPI.
# Never prefix these with NEXT_PUBLIC_ — that would ship the key to the browser.
BACKEND_API_URL=http://localhost:8000
BACKEND_API_KEY=
